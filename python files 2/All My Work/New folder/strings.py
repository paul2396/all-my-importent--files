def new_func():
    words = ["car", "dog", "metamorphose", "disagreement"]
    for w in words:
        print(w, len(w))
#!=========================================
def new_func():
    words = ["car", "dog", "metamorphose", "disagreement"]
    for w in words:
        print(w, len(w))
#!===========================================
# Snippet 12
def new_func():
    words = ["car", "dog", "metamorphose", "disagreement"]
    for w in words:
        print(w, len(w))

# Call the function
new_func()
#!==========================================